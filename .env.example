# 只有需要覆盖默认配置时才把这个文件复制成 .env
# 默认配置开箱即用：数据库生成在项目根目录下的 ./sany_financials.db
# 注意是相对「项目根目录」解析，不是相对「当前工作目录」

# 运行环境。**不设时默认为 production（安全默认：debug 关闭、不回传数据库绝对路径）**。
# 本地调试想要热重载与调试器，取消下面这行的注释即可。
# APP_ENV=development

# 可选。填相对路径时会按项目根目录解析。
# DATABASE_PATH=./sany_financials.db
